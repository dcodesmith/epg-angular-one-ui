// import ChannelService from './channel.service.es6';
// import AddChannelDirective from './add-channel.directive';

// export default function (app) {
  // require('./add-channel.directive')(app);
  // app.directive(AddChannelDirective.name, AddChannelDirective);
  // app.service(ChannelService.name, ChannelService);
// }
